{
  "watch": ["src", ".env"],
  "ext": "js,ts,json",
  "ignore": ["src/log/*", "src/**/*.{spec, test}.ts"],
  "exec": "ts-node -r tsconfig-paths/register --transpile-only -r dotenv/config src/main.ts",
  "env": {
    "DOTENV_CONFIG_PATH": ".env.local"
  }
}
